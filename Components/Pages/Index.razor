@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirige automáticamente la raíz hacia tu nueva ruta
        Navigation.NavigateTo("/MDP/Inicio", forceLoad: false);
    }
}